<%= form_with(model: comnent, local: true) do |form| %>
  <% if comnent.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(comnent.errors.count, "error") %> prohibited this comnent from being saved:</h2>

      <ul>
        <% comnent.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :post_id %>
    <%= form.text_field :post_id %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
